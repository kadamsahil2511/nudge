# Optional: the current local-first application works without Supabase credentials.
# Add these in Vercel Project Settings when enabling the Supabase client.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
